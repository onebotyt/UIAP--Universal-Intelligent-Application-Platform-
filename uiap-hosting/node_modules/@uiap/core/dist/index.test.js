import { describe, it, expect } from 'vitest';
import { CORE_VERSION, PLATFORM_NAME, PLATFORM_FULL_NAME } from './index.js';
describe('@uiap/core', () => {
    it('exports the correct version', () => {
        expect(CORE_VERSION).toBe('0.1.0');
    });
    it('exports the platform name', () => {
        expect(PLATFORM_NAME).toBe('UIAP');
    });
    it('exports the full platform name', () => {
        expect(PLATFORM_FULL_NAME).toBe('Universal Intelligent Application Platform');
    });
});
//# sourceMappingURL=index.test.js.map