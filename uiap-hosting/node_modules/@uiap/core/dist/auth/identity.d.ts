export interface User {
    id: string;
    username: string;
    password_hash: string;
    is_active: boolean;
    requires_2fa: boolean;
    totp_secret: string | null;
}
export interface Permission {
    module_name: string;
    action: string;
}
export declare function getUserByUsername(username: string): Promise<User | null>;
export declare function getUserById(id: string): Promise<User | null>;
export declare function getUserPermissions(userId: string): Promise<Permission[]>;
export declare function hashPassword(password: string): Promise<string>;
export declare function verifyPassword(password: string, hash: string): Promise<boolean>;
//# sourceMappingURL=identity.d.ts.map