import { AsyncLocalStorage } from 'async_hooks';
export interface TenantContext {
    id: string;
    databaseUrl: string;
}
export declare const tenantContextStorage: AsyncLocalStorage<TenantContext>;
/**
 * Executes a callback within a specific tenant context.
 * Useful for middleware to wrap the entire request.
 */
export declare function runWithTenant<T>(tenant: TenantContext, callback: () => T): T;
/**
 * Retrieves the current tenant context.
 * Returns null if running in single-tenant (Local Edge) mode.
 */
export declare function getTenantContext(): TenantContext | null;
//# sourceMappingURL=tenant.d.ts.map