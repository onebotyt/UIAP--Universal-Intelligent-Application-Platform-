import * as crypto from 'crypto';
/**
 * Generates an Ed25519 keypair in PEM format.
 *
 * The private key must NEVER be stored in the repository, Edge installation,
 * or audit logs. It is used exclusively by the developer signing CLI.
 */
export function generateKeyPair() {
    const { publicKey, privateKey } = crypto.generateKeyPairSync('ed25519', {
        publicKeyEncoding: { type: 'spki', format: 'pem' },
        privateKeyEncoding: { type: 'pkcs8', format: 'pem' },
    });
    return { publicKey, privateKey };
}
/**
 * Signs a package digest with an Ed25519 private key.
 *
 * @param digest - The hex-encoded SHA-256 package digest
 * @param privateKeyPem - PEM-encoded Ed25519 private key
 * @returns Base64-encoded Ed25519 signature
 */
export function signPackageDigest(digest, privateKeyPem) {
    const sign = crypto.sign(null, Buffer.from(digest, 'utf8'), privateKeyPem);
    return sign.toString('base64');
}
//# sourceMappingURL=sign.js.map