/**
 * Module signature verification interface.
 *
 * The production implementation is PackageVerifier which performs
 * full 10-step cryptographic verification using Ed25519.
 */
export interface ModuleSignatureVerifier {
    verify(buffer: Buffer): Promise<{
        valid: boolean;
        error?: string;
        manifest?: Record<string, unknown>;
    }>;
}
//# sourceMappingURL=verifier.d.ts.map