import { TrustedKeyStore } from './TrustedKeyStore.js';
/**
 * Structured result from package verification.
 */
export interface VerificationResult {
    valid: boolean;
    error?: string;
    /** One of the defined rejection reasons for audit logging */
    reason?: 'invalid_zip' | 'invalid_manifest' | 'invalid_structure' | 'invalid_signature_metadata' | 'untrusted_key' | 'hash_mismatch' | 'digest_mismatch' | 'signature_invalid' | 'core_version_mismatch' | 'invalid_dependencies';
    manifest?: Record<string, unknown>;
}
/**
 * Production-quality 10-step package verification boundary.
 *
 * All verification occurs BEFORE extraction. If any step fails,
 * the package is rejected atomically — no partial installation.
 */
export declare class PackageVerifier {
    private readonly keyStore;
    constructor(keyStore?: TrustedKeyStore);
    /**
     * Verifies a module package ZIP buffer through the complete
     * 10-step verification pipeline.
     */
    verify(buffer: Buffer): Promise<VerificationResult>;
}
//# sourceMappingURL=PackageVerifier.d.ts.map