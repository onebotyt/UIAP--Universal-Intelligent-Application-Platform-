export interface DeviceEventEnvelope {
    eventId: string;
    deviceId: string;
    eventType: string;
    occurredAt: string;
    payload: unknown;
}
export declare class DeviceEventRouter {
    private static readonly MAX_EVENT_AGE_MS;
    static processEvent(envelope: DeviceEventEnvelope): Promise<'accepted' | 'rejected' | 'duplicate'>;
}
//# sourceMappingURL=events.d.ts.map