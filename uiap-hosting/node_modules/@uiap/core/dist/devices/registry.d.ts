export interface DeviceRecord {
    id: string;
    hardware_id: string;
    name: string;
    type: string;
    status: 'REGISTERED' | 'ONLINE' | 'OFFLINE' | 'DISABLED';
    enabled: boolean;
    last_heartbeat_at: Date | null;
    created_at: Date;
    updated_at: Date;
}
export interface RegisterDeviceParams {
    hardwareId: string;
    name: string;
    type: string;
    secret: string;
}
export declare class DeviceRegistry {
    static registerDevice(params: RegisterDeviceParams): Promise<{
        device: DeviceRecord;
        uiapDeviceId: string;
    }>;
    static getDevice(id: string): Promise<DeviceRecord | null>;
    static getDeviceByHardwareId(hardwareId: string): Promise<DeviceRecord | null>;
    static listDevices(): Promise<DeviceRecord[]>;
    static enableDevice(id: string): Promise<void>;
    static disableDevice(id: string): Promise<void>;
    static updateHeartbeat(id: string): Promise<void>;
}
//# sourceMappingURL=registry.d.ts.map