import { getDb } from '../db/pool.js';
import * as bcrypt from 'bcrypt';
import crypto from 'crypto';
export class DeviceRegistry {
    static async registerDevice(params) {
        const { hardwareId, name, type, secret } = params;
        const credentialHash = await bcrypt.hash(secret, 10);
        const db = getDb();
        const id = crypto.randomUUID();
        await db('core_device_registry').insert({
            id,
            hardware_id: hardwareId,
            name,
            type,
            status: 'REGISTERED',
            enabled: true,
            credential_hash: credentialHash,
        });
        const device = await db('core_device_registry').where({ id }).first();
        return {
            device,
            uiapDeviceId: device.id,
        };
    }
    static async getDevice(id) {
        const db = getDb();
        const device = await db('core_device_registry')
            .select('id', 'hardware_id', 'name', 'type', 'status', 'enabled', 'last_heartbeat_at', 'created_at', 'updated_at')
            .where({ id })
            .first();
        return device || null;
    }
    static async getDeviceByHardwareId(hardwareId) {
        const db = getDb();
        const device = await db('core_device_registry')
            .select('id', 'hardware_id', 'name', 'type', 'status', 'enabled', 'last_heartbeat_at', 'created_at', 'updated_at')
            .where({ hardware_id: hardwareId })
            .first();
        return device || null;
    }
    static async listDevices() {
        const db = getDb();
        return await db('core_device_registry')
            .select('id', 'hardware_id', 'name', 'type', 'status', 'enabled', 'last_heartbeat_at', 'created_at', 'updated_at')
            .orderBy('created_at', 'desc');
    }
    static async enableDevice(id) {
        const db = getDb();
        await db('core_device_registry')
            .where({ id })
            .update({
            enabled: true,
            updated_at: db.fn.now()
        });
    }
    static async disableDevice(id) {
        const db = getDb();
        await db('core_device_registry')
            .where({ id })
            .update({
            enabled: false,
            status: 'DISABLED',
            updated_at: db.fn.now()
        });
    }
    static async updateHeartbeat(id) {
        const db = getDb();
        await db('core_device_registry')
            .where({ id, enabled: true })
            .update({
            last_heartbeat_at: db.fn.now(),
            status: 'ONLINE',
            updated_at: db.fn.now()
        });
    }
}
//# sourceMappingURL=registry.js.map