export declare class EventRecovery {
    private isRunning;
    private timer;
    private readonly intervalMs;
    private readonly staleTimeoutSeconds;
    start(): void;
    stop(): void;
    private loop;
    /**
     * Finds events stuck in PROCESSING state longer than staleTimeoutSeconds
     * and reverts them to PENDING so they can be re-attempted.
     */
    recoverStaleEvents(): Promise<number>;
}
//# sourceMappingURL=EventRecovery.d.ts.map