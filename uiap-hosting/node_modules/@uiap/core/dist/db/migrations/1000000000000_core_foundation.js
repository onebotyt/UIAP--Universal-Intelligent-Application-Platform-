export const shorthands = undefined;
export async function up(pgm) {
    // Create core schema to namespace platform tables
    pgm.createSchema('core', { ifNotExists: true });
    // 1. Configurations
    pgm.createTable({ schema: 'core', name: 'configurations' }, {
        key: { type: 'varchar(255)', primaryKey: true },
        value: { type: 'text', notNull: true },
        updated_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // 2. Module Installations
    pgm.createTable({ schema: 'core', name: 'module_installations' }, {
        id: { type: 'varchar(255)', primaryKey: true },
        version: { type: 'varchar(50)', notNull: true },
        is_enabled: { type: 'boolean', notNull: true, default: false },
        installed_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
        updated_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // 3. Device Registry
    pgm.createTable({ schema: 'core', name: 'device_registry' }, {
        id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
        hardware_id: { type: 'varchar(255)', notNull: true, unique: true },
        name: { type: 'varchar(255)', notNull: true },
        status: { type: 'varchar(50)', notNull: true, default: 'pending' },
        last_heartbeat_at: { type: 'timestamp' },
        created_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
        updated_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // 4. Audit Logs (Append-only)
    pgm.createTable({ schema: 'core', name: 'audit_logs' }, {
        id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
        actor_id: { type: 'uuid' }, // Nullable for system actions
        action: { type: 'varchar(255)', notNull: true },
        entity_type: { type: 'varchar(255)' },
        entity_id: { type: 'varchar(255)' },
        details: { type: 'jsonb' },
        ip_address: { type: 'varchar(45)' },
        created_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // Add indexes for audit logs
    pgm.createIndex({ schema: 'core', name: 'audit_logs' }, 'actor_id');
    pgm.createIndex({ schema: 'core', name: 'audit_logs' }, 'action');
    pgm.createIndex({ schema: 'core', name: 'audit_logs' }, 'created_at');
}
export async function down(pgm) {
    pgm.dropTable({ schema: 'core', name: 'audit_logs' });
    pgm.dropTable({ schema: 'core', name: 'device_registry' });
    pgm.dropTable({ schema: 'core', name: 'module_installations' });
    pgm.dropTable({ schema: 'core', name: 'configurations' });
    pgm.dropSchema('core');
}
//# sourceMappingURL=1000000000000_core_foundation.js.map