export const shorthands = undefined;
export async function up(pgm) {
    // 1. Users
    pgm.createTable({ schema: 'core', name: 'users' }, {
        id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
        username: { type: 'varchar(255)', notNull: true, unique: true },
        password_hash: { type: 'varchar(255)', notNull: true },
        is_active: { type: 'boolean', notNull: true, default: true },
        created_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
        updated_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // 2. Roles
    pgm.createTable({ schema: 'core', name: 'roles' }, {
        id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
        name: { type: 'varchar(255)', notNull: true, unique: true },
        description: { type: 'varchar(255)' },
        is_system: { type: 'boolean', notNull: true, default: false },
        created_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // 3. Permissions
    pgm.createTable({ schema: 'core', name: 'permissions' }, {
        id: { type: 'uuid', primaryKey: true, default: pgm.func('gen_random_uuid()') },
        module_name: { type: 'varchar(255)', notNull: true },
        action: { type: 'varchar(255)', notNull: true },
        description: { type: 'varchar(255)' },
        created_at: {
            type: 'timestamp',
            notNull: true,
            default: pgm.func('current_timestamp'),
        },
    });
    // Unique constraint for module_name + action
    pgm.addConstraint({ schema: 'core', name: 'permissions' }, 'core_permissions_module_action_unique', { unique: ['module_name', 'action'] });
    // 4. User Roles
    pgm.createTable({ schema: 'core', name: 'user_roles' }, {
        user_id: {
            type: 'uuid',
            notNull: true,
            references: { schema: 'core', name: 'users' },
            onDelete: 'CASCADE',
        },
        role_id: {
            type: 'uuid',
            notNull: true,
            references: { schema: 'core', name: 'roles' },
            onDelete: 'CASCADE',
        },
    });
    // Primary key for user_roles
    pgm.addConstraint({ schema: 'core', name: 'user_roles' }, 'core_user_roles_pkey', {
        primaryKey: ['user_id', 'role_id'],
    });
    // 5. Role Permissions
    pgm.createTable({ schema: 'core', name: 'role_permissions' }, {
        role_id: {
            type: 'uuid',
            notNull: true,
            references: { schema: 'core', name: 'roles' },
            onDelete: 'CASCADE',
        },
        permission_id: {
            type: 'uuid',
            notNull: true,
            references: { schema: 'core', name: 'permissions' },
            onDelete: 'CASCADE',
        },
    });
    // Primary key for role_permissions
    pgm.addConstraint({ schema: 'core', name: 'role_permissions' }, 'core_role_permissions_pkey', {
        primaryKey: ['role_id', 'permission_id'],
    });
}
export async function down(pgm) {
    pgm.dropTable({ schema: 'core', name: 'role_permissions' });
    pgm.dropTable({ schema: 'core', name: 'user_roles' });
    pgm.dropTable({ schema: 'core', name: 'permissions' });
    pgm.dropTable({ schema: 'core', name: 'roles' });
    pgm.dropTable({ schema: 'core', name: 'users' });
}
//# sourceMappingURL=1000000000001_core_identity.js.map