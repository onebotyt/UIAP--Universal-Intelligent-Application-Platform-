export async function up(pgm) {
    // 1. Create the new permissions
    pgm.sql(`
    INSERT INTO core.permissions (module_name, action, description) VALUES
      ('backup', 'view', 'Allows viewing available local backups'),
      ('backup', 'create', 'Allows creating a new local backup'),
      ('backup', 'restore', 'Allows restoring the database from a local backup'),
      ('backup', 'delete', 'Allows deleting local backup files')
    ON CONFLICT (module_name, action) DO NOTHING;
  `);
    // 2. Assign these permissions to the 'Administrator' role automatically
    pgm.sql(`
    INSERT INTO core.role_permissions (role_id, permission_id)
    SELECT r.id, p.id
    FROM core.roles r
    CROSS JOIN core.permissions p
    WHERE r.name = 'Administrator'
      AND p.module_name = 'backup' 
      AND p.action IN ('view', 'create', 'restore', 'delete')
    ON CONFLICT DO NOTHING;
  `);
}
export async function down(pgm) {
    // Remove from Administrator role
    pgm.sql(`
    DELETE FROM core.role_permissions
    WHERE permission_id IN (
      SELECT id FROM core.permissions
      WHERE module_name = 'backup' AND action IN ('view', 'create', 'restore', 'delete')
    );
  `);
    // Delete the permissions
    pgm.sql(`
    DELETE FROM core.permissions
    WHERE module_name = 'backup' AND action IN ('view', 'create', 'restore', 'delete');
  `);
}
//# sourceMappingURL=1000000000006_core_backup_permissions.js.map