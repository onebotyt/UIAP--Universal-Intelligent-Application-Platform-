export interface BackupManifest {
    formatVersion: string;
    coreVersion: string;
    createdAt: string;
    modules: Array<{
        id: string;
        version: string;
    }>;
}
export interface BackupMetadata {
    id: string;
    sizeBytes: number;
    createdAt: string;
    manifest: BackupManifest;
}
export declare class BackupManager {
    private static get backupsDir();
    static listBackups(): Promise<BackupMetadata[]>;
    static getBackup(id: string): Promise<BackupMetadata | null>;
    static createBackup(): Promise<BackupMetadata>;
    static deleteBackup(id: string): Promise<void>;
    static restoreBackup(id: string): Promise<void>;
}
//# sourceMappingURL=BackupManager.d.ts.map