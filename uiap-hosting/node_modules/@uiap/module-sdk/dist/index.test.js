import { describe, it, expect } from 'vitest';
import { MODULE_SDK_VERSION } from './index.js';
describe('@uiap/module-sdk', () => {
    it('exports the correct SDK version', () => {
        expect(MODULE_SDK_VERSION).toBe('0.1.0');
    });
    it('ModuleManifest type is usable', () => {
        const manifest = {
            id: 'test-module',
            name: 'Test Module',
            version: '1.0.0',
            description: 'A test module for verification',
            platform: 'UIAP',
        };
        expect(manifest.id).toBe('test-module');
        expect(manifest.platform).toBe('UIAP');
    });
});
//# sourceMappingURL=index.test.js.map